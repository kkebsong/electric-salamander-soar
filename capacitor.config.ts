import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.electricsalamandersoar',
  appName: 'electric-salamander-soar',
  webDir: 'dist'
};

export default config;
